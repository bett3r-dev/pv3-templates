{
  "extends": "{{nestedFolders}}tsconfig.json",
  "compilerOptions": {
    "outDir": "build",
    "composite": true,
		"rootDir": "."
  },
  "references": [
    {"path": "{{nestedFolders}}{{domainEventsPath}}"}
  ],
  "include": [
    ".",
    "package.json"
  ],
  "exclude": [
    "(?:^|/)node_modules/",
    "build"
  ]
}
