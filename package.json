{
  "name": "{{packageName}}",
  "version": "1.0.0",
  "exports": {
    "import": "./build/mjs/index.js",
    "require": "./build/cjs/index.js"
  },
  "license": "MIT",
  "scripts": {
    "clean": "yarn run -T tsc --build --clean && rm -rf build",
    "build": "yarn run -T tsc --build --verbose && yarn run -T tsc -p tsconfig.esm.json"
  }
}

