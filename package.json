{
  "name": "{{packageName}}",
  "version": "1.0.0",
  "exports": {
    "import": "./build/mjs/index.js",
    "require": "./build/cjs/index.js"
  },
  "types": "build/cjs/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn run -T tsc -p tsconfig.build-mjs.json && yarn run -T tsc -p tsconfig.build-cjs.json"
  }
}
