{
  "extends": "{{nestedFolders}}tsconfig.json",
  "compilerOptions": {
    "outDir": "build",
    "composite": true,
		"rootDir": ".",
    "lib": ["esnext", "dom"]
  },
  "references": [
    {"path": "{{nestedFolders}}{{domainEventsPath}}"}
  ],
  "include": [
    ".",
    "package.json"
  ],
  "exclude": [
    "(?:^|/)node_modules/",
    "build"
  ]
}
