{
  "name": "{{applicationName}}",
  "version": "1.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild:clean": "yarn clean",
    "build:clean": "yarn build",
    "clean": "yarn workspaces foreach -Ait run clean",
    "build": "yarn workspaces foreach -Ait run build",
    "build:update": "yarn workspaces foreach -pi run build",
    "test": "yarn node --experimental-vm-modules $(yarn bin jest)",
    "link-pv3": "yarn link -Arp ../pv3",
    "lint": "eslint --fix --ext ts .",
    "start": "./scripts/start.sh",
    "project": "node ./packages/esas-server/scripts/generateProjection.mjs"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^20.3.1",
    "@typescript-eslint/eslint-plugin": "^5.60.0",
    "@typescript-eslint/parser": "^5.60.0",
    "editorconfig": "^1.0.2",
    "eslint": "^8.43.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-jest": "^27.2.2",
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "ts-toolbelt": "^9.6.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "*"
  }
}
