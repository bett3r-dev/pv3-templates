{
  "name": "{{application}}-{{service}}",
  "version": "1.0.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "start": "yarn run -T nodemon index.ts",
    "build": "yarn run -T tsc --build --verbose"
  },
  "dependencies": {
    "@bett3r-dev/bett3r-utils": "^4.0.6",
    "@bett3r-dev/jsonschema-definer": "^1.4.4",
    "pv3": "^1.0.50",
    "ramda": "^0.29.0"
  }
}
